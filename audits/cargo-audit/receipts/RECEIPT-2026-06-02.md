# cargo-audit receipt — 2026-06-02

- **Tool:** `cargo-audit` 0.22.1 · **advisory DB:** RustSec `advisory-db` (fetched from GitHub), **1102 advisories loaded**.
- **Host:** x86_64, Linux. **zic-rs:** 0.1.0. **Command:** `cargo audit`. **Input:** `Cargo.lock` (**67 crate dependencies**). **Exit:** 0.
- **Raw:** `raw-2026-06-02.txt`.

## Result

**Clean — 0 vulnerabilities, 0 warnings.** All 67 resolved dependencies were scanned against the 1102-advisory
RustSec database; none is subject to a known advisory or yanked at scan time.

## Residual · non-claims

- **Point-in-time:** a clean run proves *no known advisory as of 2026-06-02 against the current `Cargo.lock`* —
  re-run on every tzdb-intake / dependency bump (the `docs/maintenance-policy.md` cadence). It is **not** a
  proof of *no vulnerability* (only *no known* one), and says nothing about logic bugs or `unsafe` correctness.
- **Next owner:** re-run in CI (the planned Scorecard/CI wiring, `docs/security-rewrite-evaluation.md` §E).
- **Status:** ✅ RUN (advisory) — clean.

# cargo-geiger receipt — 2026-06-02

- **Tool:** `cargo-geiger`. **Host:** x86_64, Linux. **zic-rs:** 0.1.0. **Command:** `cargo geiger --all-features`. **Exit:** 0 (4 warnings = unscanned `README.md`/`*.md` files in deps — benign). **Raw:** `raw-2026-06-02.txt`.

## Result — the `unsafe` census

- **zic-rs's own crate: 0 `unsafe`** — `#![forbid(unsafe_code)]` makes this structural, and geiger marks the
  crate accordingly (no `unsafe` fns/exprs/impls attributable to zic-rs).
- **Dependency-tree aggregate (used/total): functions 60/547 · expressions 1358/26514 · impls 14/36 ·
  traits 1/2 · methods 58/2459.** The `unsafe` lives where expected — the **syscall/runtime layers**:
  `rustix` (45/436 fns), `linux-raw-sys` (10/10), `libc` (0/92 used / 34 unsafe exprs), `getrandom` (3/7),
  `once_cell`, `syn`/`proc-macro2` (build-time). These are the standard, widely-audited foundation crates
  pulled in transitively (mostly via `tempfile` → `rustix`/`getrandom` and `clap` → `syn` at build time).

## Residual · non-claims

- geiger is a **census, not a verifier** — it counts/locates `unsafe`, it does **not** prove any `unsafe` is
  *correct*. The dependency `unsafe` surface is **not** covered by zic-rs's `#![forbid(unsafe_code)]` (that
  only governs zic-rs's own code); correctness of the foundation crates' `unsafe` is delegated to those
  projects (+ `cargo-audit`/`cargo-vet`/`miri` as cross-checks).
- A future **minimal-dependency** pass (e.g. dropping `tempfile`'s `rustix` path where feasible) would shrink
  this surface; tracked, not done.
- **Status:** ✅ RUN (advisory) — census captured; zic-rs own code 0 unsafe (forbid), dependency surface quantified.

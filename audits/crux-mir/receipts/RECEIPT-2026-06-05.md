# RECEIPT — crux-mir — state: RAN (symbolic, Overall status: Valid) — 2026-06-05

**Disposition (typed): `ran_valid` — corrects the earlier "installed but no harnesses" disposition. zic-rs-native `#[crux::test]` harnesses authored + symbolically PROVED.**

- **Tool:** crux-mir 0.12.0.0.99 (Crux 0.9.0.0.99) + mir-json (schema v11) on toolchain nightly-2025-09-14; SMT solver z3 4.15.3.
- **Toolchain libs:** the `crucible` support crate + translated-std `rlibs` were built **fresh from upstream** (`github.com/GaloisInc/mir-json`, HEAD `13232df`, pinned nightly-2025-09-14) via `mir-json-translate-libs` into a clean throwaway dir — **no external project reused**.
- **Command:** `CRUX_RUST_LIBRARY_PATH=<fresh rlibs> cargo crux-test --lib -- --solver z3` in the detached harness `audits/crux-mir/harness/` (own `[workspace]`, mirrors `fuzz/`/loom — nothing leaks into the main crate's dep/vet/audit surface). **Raw:** `raw-crux-run-2026-06-05.txt`.
- **Harnesses (zic-rs-native, mirroring the Kani reduced-surface proofs — second-engine corroboration):**
  1. `cursor_skip_cannot_truncate` — `Cursor::skip_within_remaining`: `pos<=len ∧ n<=len-pos ⇒ pos+n<=len` (cf. Kani T23.kani.2), symbolic over all `pos,n,len` (bounded `<2^40`).
  2. `type_index_guard_is_sound` — `idx<typecnt ⇒ idx` is a valid index into `types[..typecnt]` (cf. Kani T23.kani.3a), symbolic.
  3. `indicator_pairing_holds` — RFC-9636 `isut==1 ⇒ isstd==1` over octet domain {0,1} (cf. Kani T23.kani.3f.3).
- **RESULT: `[Crux] Overall status: Valid` — Total 5 · Proved 5 · Disproved 0 · Incomplete 0 · Unknown 0.** A different symbolic engine (Crucible+what4+z3) than Kani (CBMC) proves the same invariants Valid → agreement between two independent executors is stronger than either alone.
- **HONEST non-claims:** bounded symbolic execution (loop/recursion bounds + solver timeouts apply); **linear integer arithmetic only** — the nonlinear count×size product is intentionally NOT here (nonlinear bitvector multiply is intractable for any SMT solver regardless of bounds; that surface is covered by Kani's bounded proof + cargo-fuzz). The trailing `can't find crate crucible` lines in the raw log are the cosmetic **doc-test** phase (crux's toolchain doesn't link crucible for doctests), not the verification — the `--lib` symbolic run is `Valid`. Not a proof of the whole crate.
- **Next owner:** extend harnesses to more reduced-surface invariants as Kani grows.

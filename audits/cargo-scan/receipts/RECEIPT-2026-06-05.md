# RECEIPT — cargo-scan — state: RAN (confirmed non-functional stub) — 2026-06-05

**Disposition (typed): `ran_stub_confirmed` — cargo-scan was invoked; the installed Rust CLI is a non-functional stub (confirmed, not guessed).**

- **Tool:** cargo-scan (installed `cargo-scan` binary). **Command:** `cargo scan`. **Host:** x86_64 Linux. **Raw:** `raw-2026-06-05.txt`.
- **Result:** the installed `cargo-scan` Rust CLI prints *"Cargo Scan doesn't yet have a Rust CLI endpoint. Please use the Python endpoint at https://github.com/PLSysSec/cargo-scan."* — i.e. the installed binary is a **non-functional stub** that performs no scan; the functional effect/capability analyzer is a separate Python tool not set up here.
- **HONEST non-claim:** no effect/FFI/IO call-graph scan was produced (the tool can't, in this form). The coverage cargo-scan would give overlaps what **cargo-geiger** (unsafe census: zic-rs own 0 unsafe; dep unsafe localised) and the **cargo-vet** first-party reviews already provide. A real cargo-scan run needs the Python endpoint (repo clone + Python env).
- **Next owner:** an operator who sets up the cargo-scan Python endpoint.

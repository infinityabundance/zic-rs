# RECEIPT — creusot — state: RAN + PROVED (deductive, Why3+SMT) — 2026-06-05

**Disposition (typed): `proved` — creusot deductively PROVED a real zic-rs invariant via Why3 + SMT. A THIRD independent engine corroborating the same property Kani (CBMC) and crux-mir (Crucible) prove.**

- **Tool:** cargo-creusot 0.12.0-dev; creusot-std from `github.com/creusot-rs/creusot` rev `0fb82c7`. **Backend:** the **matched** why3 **1.8.2+git** (`git-2c0f2992`, gitlab.inria.fr/why3) + why3find **1.3.0+dev** (`git-eab37557`, creusot-rs/why3find), **built from source** in a clean dedicated opam switch (OCaml 5.3.0) at the exact versions creusot 0.12-dev pins (`creusot-deps.opam`). Solvers: z3 4.15.3 / alt-ergo 2.6.2 / cvc5 1.3.1.
- **Harness (zic-rs-native):** `block_len(count: u32, size: u32) -> u64` with `#[ensures(result@ == count@ * size@)]` — the count×size non-overflow underpinning `tzif::validate::checked_block_len` (T17.5 RISK.COUNT.1).
- **Command:** `why3find prove -p creusot -P z3,alt-ergo,cvc5 verif/.../block_len.coma` (matched toolchain; creusot Why3 prelude package installed into the switch's why3find site). **Raw:** `raw-discharge-2026-06-05.txt`; proof obligation `block_len.coma`.
- **RESULT:**
  ```
  Theory verif.zic_rs_creusot_harness_rlib.block_len.Coma: ✔ (1)
  Proved (verif/zic_rs_creusot_harness_rlib/block_len.coma) ✔   (rc=0)
  ```
  The verification condition (`result@ == count@ * size@`, i.e. the widened product is exact / non-wrapping) is **discharged by Why3 + SMT**.
- **The fix that got here:** the initial discharge failed on a frontend↔backend **version skew** (the installed why3 1.8.2 release couldn't parse cargo-creusot 0.12-dev's `coma`). Resolved by **building the creusot-pinned why3/why3find from source** (matched `+git`/`+dev` versions) in a clean OCaml-5.3.0 opam switch, then installing the real creusot Why3 prelude package (`int`/`prelude`/`slice` theories) into that why3find's site.
- **Cross-engine corroboration:** the same invariant is now proven by **three independent engines** — **Kani** T23.kani.1 (CBMC, bounded model-checking) · **crux-mir** (Crucible+what4+z3, symbolic execution, 5/5 Valid) · **creusot** (Why3 + SMT, deductive). Agreement across CBMC, Crucible, and Why3 is far stronger than any one alone.
- **Isolation:** the proof ran in an isolated /tmp copy of the harness (creusot walks up to the workspace root); no repo-root artifacts; the harness source + the proven `block_len.coma` are the committed record; `verif/`/`target/` are gitignored build artifacts. The matched toolchain lives in a dedicated opam switch, not the main environment.
- **HONEST non-claim:** one reduced-surface invariant is proven, not the whole crate. creusot proves *annotated* code; broad correctness is not claimed.

# cargo-auditable receipt — 2026-06-02

- **Tool:** `cargo-auditable`. **Host:** x86_64, Linux. **zic-rs:** 0.1.0. **Command:** `cargo auditable build --release`. **Exit:** 0. **Raw:** `raw-2026-06-02.txt`.

## Result

**Built a release binary with an embedded, recoverable dependency manifest** (`target/release/zic-rs`,
2,246,536 bytes). The embedded manifest lists the exact dependency tree compiled into the artifact (the same
67-crate set `cargo-audit` scanned) — so the artifact itself is auditable after the fact (e.g.
`cargo audit bin target/release/zic-rs`), without the source tree.

## Residual · non-claims

- Embedding a manifest is **provenance, not an attestation** — it records *what was compiled in*, not that
  those crates are vulnerability-free (that is `../cargo-audit/`) nor that the build was reproducible/signed
  (the SBOM/SLSA/signing items in `docs/security-rewrite-evaluation.md` §D are **planned, not present**).
- This is the in-artifact companion to the in-repo `manifest.json`/per-artifact SHA-256 provenance.
- **Status:** ✅ RUN (advisory) — embedded manifest produced.

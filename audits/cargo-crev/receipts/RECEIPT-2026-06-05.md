# RECEIPT — cargo-crev — state: RAN (empty web-of-trust) — 2026-06-05

**Disposition (typed): `ran_no_trust` — cargo-crev IS installed and RAN; the result is a real, empty web-of-trust (not a placeholder).**

- **Tool:** cargo-crev 0.27.1. **Command:** `cargo crev verify`. **Host:** x86_64 Linux. **Raw:** `raw-2026-06-05.txt`.
- **Result:** `cargo crev verify` enumerated all **66 resolved crates** in the dependency tree and reported **"There are no trusted Ids. There is nothing to verify against."** — no community crev proofs are imported and no trusted reviewers are configured, so every crate shows an empty trust column.
- **HONEST non-claim:** this is an **empty-but-real** result, not a pass — cargo-crev confirms there is no web-of-trust evidence for zic-rs's dependencies here. The first-party review layer is provided by **cargo-vet** (T23.cargo-vet.8: 42 fully · 20 first-party). cargo-crev would graduate to a real trust verdict only when an operator configures a crev id + imports a proof repository.
- **Next owner:** a networked operator with a configured crev web-of-trust.

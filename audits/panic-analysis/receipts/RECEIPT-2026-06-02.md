# panic-analysis receipt — 2026-06-02

- **Tool:** `grep -rn` (manual classification) — no external tool; project-internal static census.
- **Host:** x86_64, Linux. **zic-rs:** 0.1.0. **Status:** advisory (project-internal).
- **Input:** the `src/` tree at this commit. **Duration:** n/a (instant). **Exit:** 0 (no prohibited primitive).

## Findings (counts in `src/`)

| Primitive | Count | Disposition |
|---|---:|---|
| `panic!` | **0** | ✅ none — prohibited on every path |
| `todo!` | **0** | ✅ none |
| `unimplemented!` | **0** | ✅ none |
| `unreachable!` | **1** | internal-invariant (allowed, named) — `src/tzif/data_block.rs:184` `unsupported TZif time size {other}` (only 4/8 are valid; not reachable from untrusted input) |
| `.expect(` | **6** | all internal-invariant with a named reason: `parser.rs:58` (continuation-without-open-zone impossible by construction) · `data_block.rs:54/71` (abbreviation present / NUL-terminated after the build pass) · `data_block.rs:180` (32-bit transition time in v1 stub — guarded by the v1-carries-no-transitions policy) · `compile/transitions.rs:96/107` (non-empty finite rule set) |
| `.unwrap()` | **103** | the large majority in `#[cfg(test)]` modules; the non-test ones are guarded-by-construction. **The untrusted-input parse/compile/TZif paths return `Result`** — hostile source/bytes become a typed `Err`, per `docs/panic-policy.md` (and the T17.1a bounds-guard + T17.5 count arithmetic close the byte-decode panic surface). |

## Structural guarantees (the backstop)

- `#![forbid(unsafe_code)]` (`src/lib.rs:41`) — no `unsafe`, so no UB-class panic source.
- `overflow-checks = true` in all profiles (`Cargo.toml:44`) — arithmetic overflow is a typed/aborting
  failure, not silent wraparound; the T17.5 `CountArithmeticVerdict` checks counts *before* allocation.

## Result · residual · non-claims

- **Result:** **0 prohibited primitives** on any path; the 1 `unreachable!` + 6 `expect` are documented
  internal invariants matching `docs/panic-policy.md`'s allow-list. No policy violation found.
- **Residual:** a grep census proves *source-level* discipline, not *reachability*. A **proof** that no
  hostile input reaches a panic is owed to `../kani/` (bounded model checking of the parse/arithmetic
  harnesses) and `../miri/` (UB under the test corpus) — both `pending` (need kani / nightly).
- **Non-claims:** this is **not** a no-panic proof; not a DoS-immunity claim (`RISK.RESOURCE.1` bounds the
  input-size tail, not all CPU); not a substitute for the fuzz campaign (`fuzz/`, `pending_capture`).
- **Next owner:** `../kani/` + `../miri/` + `fuzz/` (the proving/exhaustive companions).

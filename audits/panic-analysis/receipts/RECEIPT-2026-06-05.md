# RECEIPT — panic-analysis — state: RAN (reproducible scan) — 2026-06-05

**Disposition (typed): `ran_clean` — upgrades the prior hand-written assertion to a real, re-runnable scan with captured output (`audits/panic-analysis/scan.sh`).**

- **Tool:** `audits/panic-analysis/scan.sh` (project-internal static census; grep over `src/`). **Command:** `bash audits/panic-analysis/scan.sh`. **Host:** x86_64 Linux. **Exit:** 0. **Raw:** `raw-2026-06-05.txt`.
- **Prohibited primitives (must be 0):** `panic!` **0** · `todo!` **0** · `unimplemented!` **0** — clean.
- **Allowed-with-justification:** `unreachable!` **1** · `.unwrap()` **108** · `.expect()` **9** (= **117** ledger sites; counts include test code). Backstops: `#![forbid(unsafe_code)]` present · `overflow-checks=true` in `[profile.release]`.
- **Full unwrap/expect ledger captured** in the raw output (every site, file:line) — the input to **STATIC-REVIEW.1's 3-bucket classification** (trust-boundary→`Result` · internal-invariant→document+test · unreachable-by-construction→document+assert).
- **HONEST non-claim:** this is a static census, not a proof of panic-freedom — it confirms no prohibited primitive and enumerates the justified ones; per-site invariant proof is STATIC-REVIEW.1 work (and the adversarial exercise of the no-panic-on-hostile-input claim is `cargo-fuzz` + the F1–F3 regression seeds). The prior receipt was an un-captured assertion with stale numbers; this one is reproducible.
- **Next owner:** STATIC-REVIEW.1 (the unwrap/expect ledger).

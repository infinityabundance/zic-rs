# cargo-valgrind / valgrind receipt — 2026-06-02

- **Tool:** `valgrind` (the `cargo-valgrind` subcommand is absent; `valgrind` itself is present). **Host:** x86_64, Linux **7.0.9-1-cachyos**. **zic-rs:** 0.1.0.
- **Command:** `valgrind --leak-check=full --error-exitcode=42 ./target/release/zic-rs compile --input … --out … --all-supported`. **Exit:** 132 (SIGILL). **Raw:** `raw-2026-06-02.txt`.

## Result — INCONCLUSIVE (environment incompatibility, not a zic-rs finding)

valgrind terminated with **SIGILL (signal 4) during early process startup — `total heap usage: 0 allocs, 0
frees`**, i.e. it died *before* zic-rs ran any code. Reproduced on a **generic-CPU build** (`RUSTFLAGS=-C
target-cpu=x86-64`) and — decisively — **on `/bin/true`**: `valgrind /bin/true` also raises SIGILL
(`"Valgrind will now raise a SIGILL signal"` · `Illegal opcode`). So **valgrind cannot execute *any* binary in
this environment** — an incompatibility between this valgrind build and the bleeding-edge cachyos kernel/glibc
(an instruction in the loader/glibc ifunc path that valgrind's core does not implement). **It is not a memory
finding about zic-rs** (the ERROR SUMMARY's "0 errors" reflects that nothing ran).

## Residual · non-claims

- **No memcheck verdict** is admitted here (neither clean nor dirty) — the run is inconclusive by environment.
  A real receipt needs a valgrind/kernel/glibc combination that runs (or a static musl build) — an operator
  step on a compatible host.
- The runtime memory-safety story is meanwhile carried by **`../miri/`** (clean over the tzif core, no UB) +
  `#![forbid(unsafe_code)]` + `overflow-checks`; valgrind would be an additional independent cross-check.
- **Status:** ◐ RUN (inconclusive — environment-incompatible; valgrind SIGILLs on `/bin/true` here).

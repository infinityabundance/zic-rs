# cargo-vet receipt — 2026-06-02 — T23.cargo-vet.1 (store initialized)

- **Tool:** `cargo-vet`. **Host:** x86_64, Linux. **zic-rs:** 0.1.0. **Commands:** `cargo vet init` → `cargo vet`. **Exit:** 0. **Raw:** `raw-2026-06-02.txt`.
- **Store created:** `supply-chain/{config.toml, audits.toml, imports.lock}` (now in the repo — the supply-chain review surface).

## Result — the review surface exists; NO verdict is claimed

`cargo vet init` recorded the **66 third-party dependencies** (the tree minus the `zic-rs` root) as
**`exemptions`** — i.e. **grandfathered-in as UNAUDITED**, not reviewed. `cargo vet` then reports *"Vetting
Succeeded (66 exempted)"* — which means **only** that every dependency is *accounted for* (either audited or
explicitly exempted), **NOT that any dependency has been audited**. `audits.toml` contains **0 audits**.

So T23.cargo-vet.1 turns the prior state (`tool runs, no store, no verdict`) into: **an initialized supply-chain
review surface, with all 66 deps honestly marked unaudited/needs-review.** That is the intended baseline — a
place to *record* future review, not a claim that review happened.

## Boundary · non-claims

- **`exemptions` ≠ `audits`.** A passing `cargo vet` here is the *exemption* path, not the *verified* path.
  **No dependency is claimed reviewed.** zic-rs makes no "supply chain verified" claim.
- **Next step (T23.cargo-vet.2, on demand):** import trusted audit sets (e.g. the Mozilla/Google `cargo vet` imports)
  and/or perform first-party audits, then **shrink the exemption list** toward real `audits.toml` entries —
  each move recorded. A meaningful "vetted" claim requires that, not `init`.
- Even fully green, cargo-vet records **review, not correctness** (vetted ≠ bug-free); the minimal
  dependency set (`clap`, `thiserror`, `tempfile`; `tz-rs` dev/optional) keeps this tractable.
- **Status:** ◐ **RUN — store initialized; 66/66 deps exempted (unaudited); 0 audits.** No verdict overclaimed.
- **Cross-reference:** `docs/security-rewrite-evaluation.md` §D · `docs/maintenance-policy.md` §5 · `../cargo-audit/` (advisory scan, clean).

# audits/cargo-vet — supply-chain audit imports

> **Status: ◐ RUN (T23.cargo-vet.8) — `cargo vet` Vetting Succeeded: 42 fully audited · 1 partial · 23
> exempted.** Of the fully audited, **20 are first-party** (full-source review by us); the rest are trusted
> imports. **23 dependencies remain honestly exempted / UNAUDITED** (the large syscall/serde/clap/proc-macro
> tiers — several not even host-reachable; reachability recorded in pass8) — no "supply-chain verified" claim
> is made. An audit folder existing is not an audit result — a result is admitted only by a receipt in
> `receipts/` (tool · version · host · command · input+hash · duration · exit · findings · fixed · residual
> · non-claims · next-owner). See `../README.md`.

- **Scope (what it witnesses for zic-rs):** which dependencies have been vetted (by us or via imported trusted audits)
- **Command:** `cargo vet`
- **Tool / version:** cargo-vet; T23.cargo-vet.1 `cargo vet init`, T23.cargo-vet.2 `cargo vet import {mozilla,google,bytecodealliance}` + `prune`, T23.cargo-vet.3 `cargo vet certify … --criteria safe-to-deploy`, run 2026-06-02.
- **Input / corpus:** `Cargo.lock` (66 third-party deps) → the `supply-chain/` store + 3 pinned imported audit sets (`imports.lock`) + 4 first-party `audits.toml` entries.
- **Result:** **T23.cargo-vet.1** — store created, 66 deps exempted (`receipts/RECEIPT-2026-06-02.md`). **T23.cargo-vet.2** — imported Mozilla/Google/Bytecode-Alliance published audits → **22 fully + 1 partially audited via delegated trust; 66→43 exempted** (`receipts/RECEIPT-2026-06-02-pass2.md`). **T23.cargo-vet.3** — **first-party audits** of 4 tiny zero-`unsafe` crates (`cfg-if`, `colorchoice`, `is_terminal_polyfill`, `once_cell_polyfill`), each **read in full** → `safe-to-deploy`; **43→39 exempted** (`receipts/RECEIPT-2026-06-02-pass3-first-party-audits.md`). **T23.cargo-vet.4** — **first-party audits** of 3 more low-risk crates (`anstyle`, `fastrand` [`#![forbid(unsafe_code)]`], `clap_lex`), each **read in full** with every `unsafe` site reviewed → `safe-to-deploy`; **39→36 exempted** (`receipts/RECEIPT-2026-06-04-pass4.md`); also added `[policy.zic-rs] audit-as-crates-io=false` (post-publish config fix). **T23.cargo-vet.5** — first-party audits of the **unsafe-heavy** tier `itoa` (13 sites, lookup-table writes), `anstyle-parse` (3 sites: array-of-MaybeUninit + 2 `u8→enum` transmutes, **16-variant invariant verified**), `anstyle-query` (1 site, `cfg(windows)` console FFI — platform note recorded), each `unsafe` site **reasoned sound** (not just counted) → **36→33 exempted** (`receipts/RECEIPT-2026-06-05-pass5.md`); deferred `semver` + `log` to .6. **T23.cargo-vet.6** — first-party audits of the two deferred harder deps: **`semver`** (49 sites; the bit-tagged `Identifier` SSO in `identifier.rs` — invariant stated, owned-buffer `Send`/`Sync`, `#![deny(unsafe_op_in_unsafe_fn)]`) and **`log`** (8 sites; the global-logger `STATE` atomic state-machine with Release/Acquire ordering + `'static` lifetime, the `_racy`/no-atomic sites platform-gated/uncalled), each site reasoned sound → **33→31 exempted** (`receipts/RECEIPT-2026-06-05-pass6.md`); both enter the graph **only on WASI targets** (via `getrandom`'s WASI backend) — never compiled in zic-rs's actual builds. **T23.cargo-vet.7** (part of T23.AUDIT-BOARD.1) — the small/low-unsafe tranche: **`thiserror`** (direct dep, 0 unsafe, benign cfg-probe build.rs) + **`thiserror-impl`** (0 *real* unsafe — the hit is a `"unsafe"` keyword string in its scanner) + **`id-arena`** (`#![forbid(unsafe_code)]`) + **`unicode-ident`** (2 trie-table `get_unchecked`, offset bounded by construction) + **`windows-link`** (Windows-only `raw-dylib` linkage macro, 0 unsafe) → **31→26 exempted** (`receipts/RECEIPT-2026-06-05-pass7.md`). **T23.cargo-vet.8** — a bounded review of the large dependency tail (syscall/proc-macro/serde/clap/platform), admitting only the small fully-understood crates and recording **host-vs-all-target reachability** for the rest: **`errno`** (12 unsafe — `valid_up_to()`-bounded `from_utf8_unchecked`, `strerror_r`+stack-buf+`strlen` slice, thread-local errno deref), **`anstream`** (only 3 unsafe — a `from_utf8_unchecked` that is *checked* under `debug_assertions`, on a printable/UTF-8-continuation-bounded slice), **`anstyle-wincon`** (2 `cfg(windows)` Win32-FFI sites) → **26→23 exempted** (`receipts/RECEIPT-2026-06-05-pass8.md`); the large/not-host-reachable tiers (`serde*` are all-target/dev-graph only, `syn`/`clap_builder`/`rustix`/`libc`/`linux-raw-sys`/`windows-sys`/…) stay exempted. Net: **20 first-party audited · ~22 trusted-import · 23 still UNAUDITED** (`cargo vet`: 42 fully + 1 partial + 23 exempted).
- **Cannot witness:** bug absence — vetting records review, not correctness
- **Non-claims:** vetted ≠ bug-free; the minimal dependency set keeps this tractable
- **Environment requirement:** a configured cargo-vet store + imported audits
- **Receipt:** `RECEIPT-2026-06-02.md` (.1) + `…-pass2.md` (.2) + `…-pass3-first-party-audits.md` (.3) + `RECEIPT-2026-06-04-pass4.md` (.4) + `RECEIPT-2026-06-05-pass5.md` (.5) + `RECEIPT-2026-06-05-pass6.md` (.6, `semver`+`log`) + `RECEIPT-2026-06-05-pass7.md` (.7, thiserror/thiserror-impl/id-arena/unicode-ident/windows-link). **T23.cargo-vet.8 (next):** the larger remaining tiers (`clap`/`serde`/`syn`/`rustix`/`libc`/`windows-sys`/…) — on demand; nothing admitted without a full-source review.
- **Cross-reference:** `docs/security-rewrite-evaluation.md` §D · `docs/maintenance-policy.md` §5

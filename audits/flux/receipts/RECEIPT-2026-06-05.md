# RECEIPT — flux — state: ran_clean_no_annotations — 2026-06-05

**Disposition (typed): `ran_clean_no_specs` — flux IS installed and RAN over zic-rs to completion, but no refinement property is claimed (no annotations authored).**

- **Tool:** flux `4d329f2` (2026-05-23). **Command:** `cargo flux`. **Host:** x86_64 Linux. **Exit:** 0. **Raw:** `raw-2026-06-05.txt`.
- **Result:** `cargo flux` compiled the dependency graph and **checked `zic-rs v0.1.0` to completion** (`Finished flux profile in 2.24s`) with **zero flux diagnostics/refinement errors**. That zic-rs even type-checks under flux's refinement driver is itself non-trivial (many crates fail flux's toolchain).
- **HONEST non-claim:** zic-rs carries **no `#[flux::...]` refinement annotations**, so a clean pass means flux found nothing to flag against its *default/inferable* refinements — it does **NOT** prove any authored refinement property. A real flux verification would require writing index/arithmetic refinement specs (the index/count-arithmetic safety zic-rs cares about is currently proven by **Kani** [count-arithmetic + bounds] + **miri**). This is a confirmed run, not a verification claim.
- **Next owner:** a refinement-types engineer who authors `#[flux::...]` specs on the count/index surface.

# RECEIPT — hax — state: RAN (frontend extraction; no backend proofs) — 2026-06-05

**Disposition (typed): `ran_frontend_extraction_no_proofs` — CONFIRMED run (corrects an earlier wrong "absent": hax is installed as `cargo hax`, not `hax`).**

- **Tool:** hax — `cargo-hax` + `hax-rust-engine` + `driver-hax-frontend-exporter` + the OCaml hax-engine. **Command:** `cargo hax json -o /tmp/hax-zic.json`. **Host:** x86_64 Linux. **Exit:** 0. **Raw:** `raw-2026-06-05.txt`.
- **Result:** hax's custom rustc driver **checked `zic-rs v0.1.0` to completion** and **exported the crate's typed AST as JSON — 185,989 bytes** (`hax-zic-frontend-2026-06-05.json`). The frontend extraction succeeds on zic-rs (non-trivial — hax targets a pure subset of Rust).
- **HONEST non-claim:** this is the **frontend extraction** only. **No proof-assistant backend extraction (F*/Coq/Lean/SSProve) and no formal proof is authored or claimed** — translating to a backend + writing/discharging proofs is a real verification project, and zic-rs's fs/IO/process surface is outside hax's pure-verification target. Confirmed: tool present + runs + extracts; gap is "no backend proofs authored." Formal-correctness proofs zic-rs *does* have are **Kani's** (10 bounded).
- **Next owner:** a formal-methods engineer who selects a backend + authors proofs; experimental.

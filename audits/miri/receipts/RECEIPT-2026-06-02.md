# miri receipt — 2026-06-02

- **Tool:** `miri` (nightly component, `cargo +nightly miri test`). **Host:** x86_64, Linux. **zic-rs:** 0.1.0.
- **Command:** `MIRIFLAGS="-Zmiri-disable-isolation" cargo +nightly miri test --lib tzif`. **Exit:** 0. **Raw:** `raw-2026-06-02.txt`.
- **Scope:** the **`tzif` byte-handling core** under the MIR interpreter — the highest-value UB surface (parse,
  RFC-9636 validate, data-block build/decode, writer round-trip). 16 unit tests run; 113 other lib tests filtered out.

## Result

**16 passed · 0 failed — miri detected NO undefined behaviour.** Including the load-bearing hostile-input guards:

- `out_of_range_transition_type_index_is_rejected_not_panic`
- `implausibly_large_declared_count_is_rejected_not_ooming`
- `count_block_len_is_checked_arithmetic`
- `type_index_out_of_bounds_is_caught_as_a_violation` · `bad_magic_is_a_violation_not_a_panic` · `empty_input_is_a_violation` · `truncated_header_is_a_violation`
- `round_trip_utc` · `round_trip_fixed_offset` · the abbreviation suffix-sharing packer tests.

This is the **proving companion** to `../panic-analysis/` (which is a static census): miri *executes* the
bounds-guarded parser over the test corpus and confirms no out-of-bounds read, use-after-free, invalid-value,
or other UB — consistent with `#![forbid(unsafe_code)]` leaving no `unsafe` to mis-use.

## Residual · non-claims

- **Scope-bounded:** run on the `tzif` core (the byte-decode surface). A full `--lib`/`--tests` miri run is a
  larger pass (the integration tests spawn the binary / use the oracle and cannot run under miri's model;
  `-Zmiri-disable-isolation` was needed for the fs-touching unit tests). The unrun modules are not claimed.
- miri checks **UB**, not **logic** (CORE.1/the oracle own correctness) or **performance** (T22).
- A clean miri run over the test corpus is **not** a proof over *all* inputs — that is the bounded-model-check
  job of `../kani/` (a proof harness over symbolic input), which is the named next step.
- **Status:** ✅ RUN (advisory) — clean over the tzif core.

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://zic-rs/schemas/zic-rs-aux-table-validation-v1.schema.json",
  "title": "Auxiliary-table validation report (zic-rs-aux-table-validation-v1)",
  "description": "Structural admissibility of zone.tab/zone1970.tab/zonenow.tab/iso3166.tab; names the universe; same-release country-code authority; not a compile input. Published contract scope: this pins the schema **identity** (the `schema` const) and the top-level object shape. Field-level tightening and full instance-validation against a JSON-Schema validator are a tracked audit-suite item (the core crate carries no validator dependency); see docs/schema-compatibility-policy.md for the versioning rules and tests/schemas.rs for the registry/drift guard.",
  "type": "object",
  "required": ["schema"],
  "properties": {
    "schema": { "const": "zic-rs-aux-table-validation-v1", "description": "the immutable schema identity; bumped only on a meaning change" }
  },
  "additionalProperties": true
}
